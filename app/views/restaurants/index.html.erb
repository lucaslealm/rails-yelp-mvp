<h1>Restaurants</h1>
<br>
<div class="list-group">
<% @restaurants.each do |restaurant| %>
  <%= link_to "#{restaurant.name.capitalize}", restaurant_path(restaurant), class: "list-group-item list-group-item-action" %>
  <br>
<% end %>
</div>
